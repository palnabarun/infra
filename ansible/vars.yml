apt_repositories:
  - name: ppa:wireguard/wireguard

system_packages:
  - build-essential
  - git
  - nginx
  - libpq-dev
  - python-pip
  - python3-pip
  - postgresql
  - supervisor
  - docker.io
  - apache2-utils
  - golang-go
  - weechat
  - tmux
  - wireguard
  - mosh

python_packages:
  - virtualenv
  - pipenv
  - virtualenvwrapper

users:
  - name: nabarun
    authorized_keys:
      - authorized_keys/nabarun_id_rsa.pub
    groups: sudo
    files: []
